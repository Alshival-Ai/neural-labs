# Store the real file at <tenant>/secrets/gateway.env with mode 0600.
# Values below are placeholders and must not be used in a running cell.
OPENCLAW_GATEWAY_TOKEN=REPLACE_WITH_UNIQUE_RANDOM_TOKEN
TEAM_MCP_URL=https://mcp.example.invalid/mcp
TEAM_MCP_TOKEN=REPLACE_WITH_TENANT_SCOPED_TOKEN

# Add only provider/channel credentials authorized for this tenant.
# OPENAI_API_KEY=REPLACE_ME
# TELEGRAM_BOT_TOKEN=REPLACE_ME
